# 04-示例代码

本页只收录能定位到真实文件与 target 的示例来源。编号文档负责 canonical 说明；这里负责告诉你“哪份源码可以直接构建运行”。

## 1. 已验证示例总表

| 场景 | 来源文件 | target | 运行命令 | 环境变量 | 状态 |
|---|---|---|---|---|---|
| 基本接入 | `examples/include/E1-basic_usage.cpp` | `E1-BasicUsage` | `cmake --build build-examples --target E1-BasicUsage && ./build-examples/examples/E1-BasicUsage` | 无 | 已验证 |
| System / Process | `examples/include/E2-system_process.cpp` | `E2-SystemProcess` | `cmake --build build-examples --target E2-SystemProcess && ./build-examples/examples/E2-SystemProcess` | 无 | 已验证 |
| Parser / Balancer | `examples/include/E3-parser_balancer.cpp` | `E3-ParserBalancer` | `cmake --build build-examples --target E3-ParserBalancer && ./build-examples/examples/E3-ParserBalancer` | 无 | 已验证 |
| 核心模块回归 | `test/test_all.cpp` | `test_all` | `cmake --build build-test --target test_all && ctest --test-dir build-test --output-on-failure` | 无 | 已验证 |
| 模块导入 | `test/module_import_smoke.cpp` | `test_import_smoke` | `cmake --build build-mod --target test_import_smoke && ctest --test-dir build-mod -R ModuleImportSmoke --output-on-failure` | 无 | 条件式验证 |

## 2. `examples/` 中的真实示例

统一构建命令：

```bash
cmake -S . -B build-examples \
  -DBUILD_MODULE_TESTS=OFF \
  -DBUILD_EXAMPLES=ON \
  -DBUILD_TESTS=OFF
```

### 2.1 `E1-BasicUsage`

- 源文件：`examples/include/E1-basic_usage.cpp`
- 覆盖模块：`StringUtils`、`System`
- 适合场景：验证 umbrella header 接入是否打通
- 额外前置：需要 `galay-kernel` 与 `concurrentqueue/moodycamel` 头文件

```bash
cmake --build build-examples --target E1-BasicUsage --parallel
./build-examples/examples/E1-BasicUsage
```

### 2.2 `E2-SystemProcess`

- 源文件：`examples/include/E2-system_process.cpp`
- 覆盖模块：`System`、`Process`、`StringUtils`
- 适合场景：核对工作目录、可执行文件路径、进程 ID 和命令输出的最小用法
- 额外前置：无额外第三方头依赖

```bash
cmake --build build-examples --target E2-SystemProcess --parallel
./build-examples/examples/E2-SystemProcess
```

### 2.3 `E3-ParserBalancer`

- 源文件：`examples/include/E3-parser_balancer.cpp`
- 覆盖模块：`ConfigParser`、`RoundRobinLoadBalancer<T>`、`WeightRoundRobinLoadBalancer<T>`、`StringUtils`
- 适合场景：从配置文本构造路由节点，并观察调度顺序
- 额外前置：无额外第三方头依赖

```bash
cmake --build build-examples --target E3-ParserBalancer --parallel
./build-examples/examples/E3-ParserBalancer
```

## 3. 测试驱动的示例来源

`test/test_all.cpp` 仍然是覆盖面最广的行为来源，但它不再是“除了唯一示例之外的唯一代码样本”。

运行说明：

- `test/CMakeLists.txt` 会优先使用 `find_package(galay-kernel CONFIG)` 导入 `galay-kernel::galay-kernel`
- macOS 会把 `galay-kernel` 的库目录写入测试 target 的 `BUILD_RPATH`
- 默认 `/usr/local` 安装下，`ctest --test-dir build-test` 不需要额外 loader 环境变量

| 主题 | 来源文件 | target | 备注 |
|---|---|---|---|
| `StringUtils` | `test/test_all.cpp` | `test_all` | 覆盖 `split`、`join`、`trim`、`toHex`、`fromHex` 等 |
| `Randomizer` | `test/test_all.cpp` | `test_all` | 覆盖范围、字符串、UUID、随机字节 |
| `System` | `test/test_all.cpp` | `test_all` | 覆盖时间、文件、目录、环境变量、主机信息 |
| `BackTrace` | `test/test_all.cpp` | `test_all` | 覆盖栈采集接口 |
| `SignalHandler` | `test/test_all.cpp` | `test_all` | 覆盖安装、查询、移除处理器 |
| `ThreadPool` / `TaskWaiter` | `test/test_all.cpp` | `test_all` | 覆盖任务执行和等待 |
| `ObjectPool` / `BlockingObjectPool` | `test/test_all.cpp` | `test_all` | 覆盖获取、释放、超时 |
| `RateLimiter` | `test/test_all.cpp` | `test_all` | 覆盖 `tryAcquire` 与协程路径 |
| `CircuitBreaker` | `test/test_all.cpp` | `test_all` | 覆盖状态转换和回退执行 |
| `Balancer` | `test/test_all.cpp` | `test_all` | 覆盖轮询、加权、随机选择 |
| `TrieTree` / `ConsistentHash` / `Mvcc` / `Huffman` | `test/test_all.cpp` | `test_all` | 覆盖主要数据结构行为 |
| `App` / `Parser` / `Process` | `test/test_all.cpp` | `test_all` | 覆盖基础参数解析、配置解析和进程操作 |

## 4. 模块导入示例

- 源文件：`test/module_import_smoke.cpp`
- target：`test_import_smoke`
- 环境变量：无

前置条件：

- CMake `>= 3.28`
- `Ninja` 或 `Visual Studio`
- 非 `AppleClang`
- `galay-kernel` 头文件与库可用
- `concurrentqueue/moodycamel` 头文件可见

当前烟雾测试覆盖：

- `import galay.utils;`
- `StringUtils::split`
- `CountingSemaphore`
- `TokenBucketLimiter`

## 5. 本页不再做的事情

- 不把没有对应源文件的长片段标成“官方示例”
- 不把 `IniParser` 当成真实类型名；公开头中的类型是 `ConfigParser`
- 不用不存在的 target 或目录名描述运行方式

## 6. 下一步

- [02-API参考](02-API参考.md)
- [05-性能测试](05-性能测试.md)
- [06-高级主题](06-高级主题.md)
